# Railway Environment Variables Template
# Copy these variables to Railway dashboard in your service settings

# =======================
# REQUIRED VARIABLES
# =======================

# Database connection (automatically set by Railway PostgreSQL plugin)
DATABASE_URL="postgresql://username:password@hostname:5432/database"

# Application environment
NODE_ENV="production"
PORT="3001"

# JWT Configuration (REQUIRED - generate a secure key)
JWT_SECRET="your-super-secret-jwt-key-minimum-32-characters-long-please-change-this"
JWT_EXPIRES_IN="7d"

# =======================
# OPTIONAL VARIABLES
# =======================

# Frontend URL for CORS (set this to your deployed frontend URL)
FRONTEND_URL="https://your-frontend-domain.vercel.app"

# Seed control (set to "true" to run seed data on first deploy)
RUN_SEED="true"

# Railway specific (automatically set by Railway)
RAILWAY_ENVIRONMENT="production"

# =======================
# INSTRUCTIONS
# =======================

# 1. Go to your Railway project dashboard
# 2. Click on your backend service
# 3. Go to "Variables" tab
# 4. Add each variable above (except DATABASE_URL - it's auto-generated)
# 5. Generate a secure JWT_SECRET using: openssl rand -base64 32
# 6. Update FRONTEND_URL with your actual frontend domain
# 7. Save and redeploy

# =======================
# SECURITY NOTES
# =======================

# - Never commit actual secrets to Git
# - Use Railway's built-in variable encryption
# - JWT_SECRET must be at least 32 characters
# - Rotate JWT_SECRET periodically for security
# - Set strong values for production 